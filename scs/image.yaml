engine:
  docker:
    from: 'ubuntu:precise'
config:
  method: puppet
dependency:
  provide:
    http:
      description: WordPress Website
      port: 80
  require:
    mysql:
      description: MySQL Database Server
      liveupdate:
        command: /scs/scs/bin/update-requires-mysql.sh
  volume:
    uploads:
      description: WordPress Uploads
